{
  "name": "capricorn-lab",
  "version": "1.0.1",
  "private": true,
  "description": "Capricorn OS — canonical hub source (D-13)",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/copy-static-assets.mjs",
    "build:pages": "npm run build",
    "deploy:hub": "node scripts/deploy-hub.mjs",
    "preview": "vite preview",
    "test": "node scripts/smoke-hub.mjs"
  },
  "devDependencies": {
    "playwright-core": "^1.61.1",
    "vite": "^6.2.0"
  },
  "dependencies": {
    "three": "^0.185.1"
  }
}
